import type { FastifyInstance } from "fastify";

// TODO(day 12): list the logged-in tenant's lead_assignment rows via @fdl/db, scoped by the auth plugin.
export async function leadsRoutes(_app: FastifyInstance) {}
